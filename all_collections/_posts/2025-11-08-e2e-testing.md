---
layout: post
title: 🧪 E2E 테스트 완벽 가이드 (Playwright, Cypress)
date: 2025-11-08
categories:
  [
    "SamBeak",
    "Testing",
    "E2E",
    "Playwright",
    "Cypress",
    "QA",
  ]
---

# E2E 테스트란 무엇인가

<br>
웹 애플리케이션을 만들었다면, <br>
실제로 잘 동작하는지 확인해야 한다. <br><br>

E2E(End-to-End) 테스트는 <br>
실제 사용자가 사용하는 것처럼 <br>
처음부터 끝까지 전체 시나리오를 테스트하는 방법이다. <br><br>

마치 자동차 공장에서 <br>
엔진, 바퀴, 문을 따로 테스트하고 (단위 테스트) <br>
조립 후 실제로 도로를 주행해보는 것 (E2E 테스트) <br>
처럼 말이다. <br><br>

> ## 왜 E2E 테스트를 해야 할까?

<br>

**이유 1: 실제 사용자 경험 검증** <br>
각 부분이 잘 동작해도 전체가 안 될 수 있다. <br><br>

**이유 2: 회귀 버그 방지** <br>
새 기능 추가 시 기존 기능이 망가지는 것을 방지. <br><br>

**이유 3: 자동화로 시간 절약** <br>
수동 테스트는 시간이 많이 걸리고 실수할 수 있다. <br><br>

**이유 4: 자신감 있는 배포** <br>
테스트가 통과하면 안심하고 배포할 수 있다. <br><br>

# 기본 개념 요약

<br>

## 🏷️ 테스트 피라미드

<br>

```
        /\
       /E2E\      적음, 느림, 비용 높음
      /------\
     /통합 테스트\   중간
    /----------\
   /  단위 테스트  \  많음, 빠름, 비용 낮음
  /--------------\
```

<br>

### 1. 단위 테스트 (Unit Test)

<br>
**개념**: 함수나 메서드 단위로 테스트 <br><br>

**예시**: <br>
더하기 함수가 2 + 3 = 5를 반환하는지 확인 <br><br>

**특징**: <br>
- 빠름 (밀리초 단위)
- 많이 작성 (수백~수천 개)
- 버그 위치 명확

<br>

### 2. 통합 테스트 (Integration Test)

<br>
**개념**: 여러 모듈이 함께 동작하는지 테스트 <br><br>

**예시**: <br>
회원가입 API가 DB에 저장되는지 확인 <br><br>

**특징**: <br>
- 중간 속도 (초 단위)
- 중간 개수 (수십~수백 개)
- 모듈 간 연동 확인

<br>

### 3. E2E 테스트 (End-to-End Test)

<br>
**개념**: 실제 사용자처럼 전체 시나리오 테스트 <br><br>

**예시**: <br>
로그인 → 상품 검색 → 장바구니 → 결제 전체 흐름 <br><br>

**특징**: <br>
- 느림 (분 단위)
- 적게 작성 (수십 개)
- 실제 사용자 경험 검증

<br>

## 🏷️ E2E 테스트 도구 비교

<br>

| 도구 | 언어 | 브라우저 | 속도 | 특징 |
|------|------|----------|------|------|
| **Playwright** | JS/TS | Chrome, Firefox, Safari | 빠름 | 최신 기술, 자동 대기 |
| **Cypress** | JS/TS | Chrome, Edge | 빠름 | 개발자 친화적, 디버깅 좋음 |
| **Selenium** | 다양 | 모든 브라우저 | 느림 | 오래됨, 안정적 |
| **Puppeteer** | JS | Chrome만 | 빠름 | 헤드리스 특화 |

<br>

## 🏷️ Playwright 핵심 개념

<br>

### 1. Browser Context

<br>
**개념**: 독립된 브라우저 세션 (쿠키, 로컬 스토리지 격리) <br><br>

**시크릿 모드 비유**: <br>
각 Context는 독립된 시크릿 창처럼 동작 <br><br>

### 2. Page

<br>
**개념**: 브라우저 탭 <br><br>

### 3. Locator

<br>
**개념**: 요소를 찾는 방법 <br><br>

```javascript
// CSS Selector
page.locator('.submit-button')

// Text로 찾기
page.locator('text=로그인')

// Role로 찾기
page.getByRole('button', { name: '제출' })
```

<br>

### 4. Auto-waiting

<br>
**개념**: 자동으로 요소가 나타날 때까지 대기 <br><br>

```javascript
// ❌ Selenium (명시적 대기 필요)
await driver.wait(until.elementLocated(By.id('button')), 5000);

// ✅ Playwright (자동 대기)
await page.click('#button'); // 자동으로 대기
```

<br>

# 실전 예시

<br>

## 🏷️ Playwright 설치 및 설정

<br>

### 1. 설치

<br>

```bash
npm init playwright@latest

# 설치 옵션
✔ Do you want to use TypeScript or JavaScript? · TypeScript
✔ Where to put your end-to-end tests? · tests
✔ Add a GitHub Actions workflow? · Yes
```

<br>

### 2. 프로젝트 구조

<br>

```
project/
├── tests/
│   ├── login.spec.ts
│   ├── checkout.spec.ts
│   └── search.spec.ts
├── playwright.config.ts
└── package.json
```

<br>

### 3. 설정 파일 (playwright.config.ts)

<br>

```typescript
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  testDir: './tests',
  
  // 타임아웃
  timeout: 30 * 1000,
  
  // 재시도
  retries: process.env.CI ? 2 : 0,
  
  // 병렬 실행
  workers: process.env.CI ? 1 : undefined,
  
  // 리포터
  reporter: 'html',
  
  use: {
    // 기본 URL
    baseURL: 'http://localhost:3000',
    
    // 스크린샷
    screenshot: 'only-on-failure',
    
    // 비디오
    video: 'retain-on-failure',
    
    // 트레이스
    trace: 'on-first-retry',
  },
  
  // 프로젝트 (브라우저별)
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
    {
      name: 'firefox',
      use: { ...devices['Desktop Firefox'] },
    },
    {
      name: 'webkit',
      use: { ...devices['Desktop Safari'] },
    },
    {
      name: 'Mobile Chrome',
      use: { ...devices['Pixel 5'] },
    },
  ],
  
  // 개발 서버 자동 시작
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:3000',
    reuseExistingServer: !process.env.CI,
  },
});
```

<br>

## 🏷️ 로그인 테스트

<br>

```typescript
import { test, expect } from '@playwright/test';

test.describe('로그인 기능', () => {
  
  test.beforeEach(async ({ page }) => {
    // 각 테스트 전에 로그인 페이지로 이동
    await page.goto('/login');
  });
  
  test('성공적인 로그인', async ({ page }) => {
    // 1. 입력 필드 찾아서 입력
    await page.fill('input[name="username"]', 'testuser');
    await page.fill('input[name="password"]', 'password123');
    
    // 2. 로그인 버튼 클릭
    await page.click('button:has-text("로그인")');
    
    // 3. 홈 페이지로 리다이렉트 확인
    await expect(page).toHaveURL('/home');
    
    // 4. 환영 메시지 확인
    await expect(page.locator('text=환영합니다')).toBeVisible();
  });
  
  test('잘못된 비밀번호', async ({ page }) => {
    await page.fill('input[name="username"]', 'testuser');
    await page.fill('input[name="password"]', 'wrongpassword');
    await page.click('button:has-text("로그인")');
    
    // 에러 메시지 확인
    await expect(page.locator('.error-message'))
      .toHaveText('아이디 또는 비밀번호가 일치하지 않습니다');
  });
  
  test('빈 필드 검증', async ({ page }) => {
    await page.click('button:has-text("로그인")');
    
    // HTML5 validation 확인
    const usernameInput = page.locator('input[name="username"]');
    await expect(usernameInput).toHaveAttribute('required', '');
  });
});
```

<br>

## 🏷️ 쇼핑몰 E2E 테스트

<br>

```typescript
import { test, expect } from '@playwright/test';

test('상품 구매 전체 흐름', async ({ page }) => {
  
  // 1. 홈페이지 방문
  await page.goto('/');
  await expect(page).toHaveTitle(/쇼핑몰/);
  
  // 2. 상품 검색
  await page.fill('input[name="search"]', '노트북');
  await page.press('input[name="search"]', 'Enter');
  
  // 검색 결과 확인
  await expect(page.locator('.product-list .product')).toHaveCount(10, {
    timeout: 5000
  });
  
  // 3. 첫 번째 상품 클릭
  await page.click('.product-list .product:first-child');
  
  // 상품 상세 페이지 확인
  await expect(page.locator('h1.product-title')).toBeVisible();
  
  // 4. 장바구니에 추가
  await page.click('button:has-text("장바구니 담기")');
  
  // 성공 토스트 메시지 확인
  await expect(page.locator('.toast.success'))
    .toHaveText('장바구니에 추가되었습니다');
  
  // 5. 장바구니로 이동
  await page.click('a[href="/cart"]');
  await expect(page).toHaveURL('/cart');
  
  // 장바구니 아이템 확인
  await expect(page.locator('.cart-item')).toHaveCount(1);
  
  // 6. 수량 변경
  await page.click('.quantity-increase');
  await expect(page.locator('.quantity-display')).toHaveText('2');
  
  // 총 가격 업데이트 확인
  const totalPrice = await page.locator('.total-price').textContent();
  expect(totalPrice).toContain('2,000,000'); // 예시
  
  // 7. 결제 페이지로 이동
  await page.click('button:has-text("주문하기")');
  await expect(page).toHaveURL('/checkout');
  
  // 8. 배송 정보 입력
  await page.fill('input[name="name"]', '홍길동');
  await page.fill('input[name="phone"]', '010-1234-5678');
  await page.fill('input[name="address"]', '서울시 강남구');
  
  // 9. 결제 수단 선택
  await page.check('input[value="card"]');
  
  // 10. 주문 완료
  await page.click('button:has-text("결제하기")');
  
  // 주문 완료 페이지 확인
  await expect(page).toHaveURL(/\/order\/\d+/);
  await expect(page.locator('.order-complete'))
    .toHaveText('주문이 완료되었습니다');
});
```

<br>

## 🏷️ API 응답 대기 및 모킹

<br>

### API 응답 대기

<br>

```typescript
test('검색 후 결과 확인', async ({ page }) => {
  await page.goto('/');
  
  // API 응답 대기
  const responsePromise = page.waitForResponse(
    response => response.url().includes('/api/search') && response.status() === 200
  );
  
  await page.fill('input[name="search"]', '노트북');
  await page.press('input[name="search"]', 'Enter');
  
  const response = await responsePromise;
  const data = await response.json();
  
  // API 응답 검증
  expect(data.results).toHaveLength(10);
  
  // UI 업데이트 확인
  await expect(page.locator('.product')).toHaveCount(10);
});
```

<br>

### API 모킹

<br>

```typescript
test('API 모킹으로 빠른 테스트', async ({ page }) => {
  // API 응답 모킹
  await page.route('**/api/products', route => {
    route.fulfill({
      status: 200,
      contentType: 'application/json',
      body: JSON.stringify({
        products: [
          { id: 1, name: '테스트 상품 1', price: 10000 },
          { id: 2, name: '테스트 상품 2', price: 20000 },
        ]
      })
    });
  });
  
  await page.goto('/products');
  
  // 모킹된 데이터로 렌더링 확인
  await expect(page.locator('.product')).toHaveCount(2);
  await expect(page.locator('.product:first-child .name'))
    .toHaveText('테스트 상품 1');
});
```

<br>

## 🏷️ Page Object Model (POM)

<br>

**개념**: 페이지별로 클래스를 만들어 재사용성 향상 <br><br>

### LoginPage 클래스

<br>

```typescript
// pages/LoginPage.ts
export class LoginPage {
  constructor(private page: Page) {}
  
  // Locators
  private usernameInput = () => this.page.locator('input[name="username"]');
  private passwordInput = () => this.page.locator('input[name="password"]');
  private loginButton = () => this.page.locator('button:has-text("로그인")');
  private errorMessage = () => this.page.locator('.error-message');
  
  // Actions
  async goto() {
    await this.page.goto('/login');
  }
  
  async login(username: string, password: string) {
    await this.usernameInput().fill(username);
    await this.passwordInput().fill(password);
    await this.loginButton().click();
  }
  
  async getErrorMessage() {
    return await this.errorMessage().textContent();
  }
  
  // Assertions
  async expectErrorMessage(message: string) {
    await expect(this.errorMessage()).toHaveText(message);
  }
}
```

<br>

### 테스트에서 사용

<br>

```typescript
import { test, expect } from '@playwright/test';
import { LoginPage } from './pages/LoginPage';

test('로그인 실패', async ({ page }) => {
  const loginPage = new LoginPage(page);
  
  await loginPage.goto();
  await loginPage.login('testuser', 'wrongpassword');
  await loginPage.expectErrorMessage('아이디 또는 비밀번호가 일치하지 않습니다');
});
```

<br>

## 🏷️ 인증 상태 재사용

<br>

**개념**: 매번 로그인하지 않고 인증 상태 저장 <br><br>

### 인증 상태 저장

<br>

```typescript
// auth.setup.ts
import { test as setup } from '@playwright/test';

const authFile = 'playwright/.auth/user.json';

setup('authenticate', async ({ page }) => {
  await page.goto('/login');
  await page.fill('input[name="username"]', 'testuser');
  await page.fill('input[name="password"]', 'password123');
  await page.click('button:has-text("로그인")');
  
  // 로그인 완료 대기
  await page.waitForURL('/home');
  
  // 인증 상태 저장
  await page.context().storageState({ path: authFile });
});
```

<br>

### 저장된 인증 상태 사용

<br>

```typescript
// playwright.config.ts
export default defineConfig({
  projects: [
    {
      name: 'setup',
      testMatch: /.*\.setup\.ts/
    },
    {
      name: 'chromium',
      use: {
        ...devices['Desktop Chrome'],
        storageState: 'playwright/.auth/user.json',
      },
      dependencies: ['setup'],
    },
  ],
});
```

<br>

```typescript
// 테스트는 이미 로그인된 상태로 시작
test('프로필 수정', async ({ page }) => {
  await page.goto('/profile'); // 이미 로그인됨
  // ...
});
```

<br>

# Best Practices

<br>

## 🏷️ 1. 테스트는 독립적이어야 함

<br>

```typescript
// ❌ Bad: 테스트 간 의존성
test('사용자 생성', async ({ page }) => {
  // user를 전역 변수에 저장
});

test('사용자 수정', async ({ page }) => {
  // 이전 테스트의 user 사용
});

// ✅ Good: 각 테스트가 독립적
test('사용자 수정', async ({ page }) => {
  // 테스트 내에서 사용자 생성
  const user = await createTestUser();
  // 수정 테스트
  // 정리
  await deleteTestUser(user.id);
});
```

<br>

## 🏷️ 2. 안정적인 Locator 사용

<br>

```typescript
// ❌ Bad: 깨지기 쉬운 Locator
page.locator('div > div:nth-child(3) > button')
page.locator('button').nth(5)

// ✅ Good: 의미 있는 Locator
page.getByRole('button', { name: '제출' })
page.getByTestId('submit-button')
page.getByLabel('사용자 이름')
```

<br>

## 🏷️ 3. 명시적인 대기 피하기

<br>

```typescript
// ❌ Bad: 고정된 대기 시간
await page.click('button');
await page.waitForTimeout(3000); // 항상 3초 대기

// ✅ Good: 자동 대기 또는 조건 대기
await page.click('button');
await page.waitForLoadState('networkidle');
await page.waitForSelector('.result');
```

<br>

## 🏷️ 4. 테스트 데이터 격리

<br>

```typescript
// ✅ Good: 고유한 테스트 데이터
test('사용자 등록', async ({ page }) => {
  const uniqueEmail = `test${Date.now()}@example.com`;
  
  await page.goto('/register');
  await page.fill('input[name="email"]', uniqueEmail);
  // ...
});
```

<br>

## 🏷️ 5. 스크린샷과 비디오 활용

<br>

```typescript
test('중요한 기능', async ({ page }) => {
  await page.goto('/dashboard');
  
  // 특정 시점 스크린샷
  await page.screenshot({ path: 'dashboard.png' });
  
  // 특정 요소만 스크린샷
  await page.locator('.chart').screenshot({ path: 'chart.png' });
  
  // 전체 페이지 스크린샷
  await page.screenshot({ path: 'full-page.png', fullPage: true });
});
```

<br>

# 실전 체크리스트

<br>

## ✅ 테스트 작성

<br>

- [ ] 핵심 사용자 흐름 커버
- [ ] 행복 경로 + 에러 케이스
- [ ] 모바일 반응형 테스트
- [ ] 크로스 브라우저 테스트

<br>

## ✅ 테스트 안정성

<br>

- [ ] 안정적인 Locator 사용
- [ ] 자동 대기 활용
- [ ] 테스트 독립성 보장
- [ ] 테스트 데이터 격리

<br>

## ✅ 성능

<br>

- [ ] 병렬 실행 설정
- [ ] 인증 상태 재사용
- [ ] 불필요한 대기 제거
- [ ] API 모킹 활용

<br>

## ✅ 디버깅

<br>

- [ ] 실패 시 스크린샷
- [ ] 실패 시 비디오 녹화
- [ ] Trace 파일 생성
- [ ] 명확한 에러 메시지

<br>

## ✅ CI/CD 통합

<br>

- [ ] GitHub Actions 설정
- [ ] 자동 재시도 설정
- [ ] 병렬 실행 최적화
- [ ] 테스트 결과 리포트

<br>

# 요약

<br>
E2E 테스트는 실제 사용자 경험을 검증하는 필수 도구다. <br><br>

**💎 핵심 포인트**:

1. **테스트 피라미드**: 단위 > 통합 > E2E 순으로 많이 작성
2. **Playwright**: 최신 기술, 빠르고 안정적
3. **독립성**: 각 테스트는 독립적으로 실행 가능
4. **안정성**: 의미 있는 Locator, 자동 대기
5. **재사용**: Page Object Model 활용
6. **효율성**: 인증 상태 재사용, API 모킹

<br>

**🎯 테스트 작성 순서**:

1. 핵심 사용자 흐름 파악
2. 행복 경로 시나리오 작성
3. 에러 케이스 추가
4. 엣지 케이스 커버

<br>

**📌 Best Practices**:

- **명확한 테스트명**: "사용자가 로그인하고 상품을 구매한다"
- **AAA 패턴**: Arrange (준비) → Act (실행) → Assert (검증)
- **단일 책임**: 하나의 테스트는 하나의 시나리오만
- **독립성**: 테스트 순서에 의존하지 않음
- **재현 가능**: 언제 실행해도 같은 결과

<br>

**🚀 Playwright vs Cypress**:

| 항목 | Playwright | Cypress |
|------|-----------|---------|
| 브라우저 | Chrome, Firefox, Safari | Chrome, Edge |
| 속도 | 빠름 | 빠름 |
| API 모킹 | 내장 | 내장 |
| 디버깅 | Trace Viewer | Time Travel |
| 러닝커브 | 낮음 | 낮음 |

<br>

E2E 테스트는 처음에는 작성 시간이 걸리지만, <br>
한 번 작성하면 수백 번 재사용할 수 있다. <br>
자동화된 테스트는 버그를 조기에 발견하고, <br>
자신감 있게 배포할 수 있게 해준다. <br><br>
